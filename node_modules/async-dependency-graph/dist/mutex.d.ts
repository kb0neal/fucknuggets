export declare class Mutex {
    private readyTest;
    private waiting?;
    constructor(readyTest: () => boolean);
    await(callback: (error?: Error) => any): void;
    ready(error?: Error): void;
}
